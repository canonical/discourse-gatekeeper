name: Integration Tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@microk8s-channel
    secrets: inherit
    with:
      modules: '["discourse", "reconcile", "conflict", "migrate"]'
      juju-channel: 3.1/stable
      microk8s-channel: 1.28-strict/stable
