name: Integration Tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@fix-charmcraft-cache
    secrets: inherit
    with:
      modules: '["discourse", "reconcile", "conflict", "migrate"]'
