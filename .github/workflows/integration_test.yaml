name: Integration Tests

on:
  push:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@do-no-tail-if-charm-artifact-not-present
    secrets: inherit
    with:
      modules: '["discourse", "reconcile", "conflict", "migrate"]'
